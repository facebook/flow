// ok, this file is windows only through multi_platform.ambient_supports_platform.directory_overrides,
// so it can import windows specific files with .windows platform suffix.
import './windows-specific';
