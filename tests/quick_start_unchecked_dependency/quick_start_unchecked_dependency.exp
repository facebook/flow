
Stop any already-running server.

Quick start.

Expect no errors.
No errors!

The Flow server is currently in lazy mode and is only checking 0/5 source files (+ 2/2 libdefs).
To learn more, visit flow.org/en/docs/lang/lazy-modes

"Warm up" a dependency.
No errors!

The Flow server is currently in lazy mode and is only checking 1/5 source files (+ 2/2 libdefs).
To learn more, visit flow.org/en/docs/lang/lazy-modes

"Perturb" a dependent that depends on the warmed up, but unchanged dependency.
No errors!

The Flow server is currently in lazy mode and is only checking 3/5 source files (+ 2/2 libdefs).
To learn more, visit flow.org/en/docs/lang/lazy-modes

Test that the perturbed dependent is actually known (expecting a crash otherwise).
No errors!

The Flow server is currently in lazy mode and is only checking 4/5 source files (+ 2/2 libdefs).
To learn more, visit flow.org/en/docs/lang/lazy-modes

Done!
