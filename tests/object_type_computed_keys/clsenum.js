// Consumed through value imports in `type_keys.js` and `value_keys.js`, where
// the imported names head a bracketed key, bare or qualified.
export class C {
  static k: 'kk' = 'kk';
}
export enum E {
  A,
}
