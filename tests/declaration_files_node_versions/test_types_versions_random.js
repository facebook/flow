// @flow

import {types_versions_random} from 'types_versions_random'
import {types_versions_random_2} from 'types_versions_random_2'

;(types_versions_random: 'ok') // no errors, no crashes
;(types_versions_random_2: 'ok') // no errors
