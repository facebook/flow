/**
 * @format
 * @flow
 */

{
  const x = 42;
  // empty line
  const y = 42;
  [x, y] as [empty, empty];
  // empty line
  // empty line
  // empty line
  // empty line
  // empty line
  [x, y] as [empty, empty];
  [x, y] as [empty, empty];
}

{
  const x = 42;
  // empty line
  // empty line
  // empty line
  const y = 42;
  [x, y] as [empty, empty];
  // empty line
  // empty line
  // empty line
  // empty line
  // empty line
  [x, y] as [empty, empty];
  [x, y] as [empty, empty];
}

{
  const x = 42;
  // empty line
  // empty line
  // empty line
  // empty line
  const y = 42;
  [x, y] as [empty, empty];
  // empty line
  // empty line
  // empty line
  // empty line
  // empty line
  [x, y] as [empty, empty];
  [x, y] as [empty, empty];
}
