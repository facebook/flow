// @flow

/**
 * Docblock for 'fred'
 *
 * @param {number} a Docblock for param a
 * @param {string} b Docblock for param b
 * @return {number} Docblock for return
 */
function fred(a: number, b: string): number {
  const x = 15;
  return 5;
}
