declare export default component(foo: string);

declare export default component<T>(foo: T);

declare export default component() renders SomeComponent;

declare export default hook (foo: string) => void;

declare export default component Named(foo: string);

declare export default hook named(foo: string): void;
