declare class Foo {
  static readonly BUFFER_SIZE = 256;
  readonly code = "NEXT_STATIC_GEN_BAILOUT";
  readonly IDENTIFIER_MAX_SIZE = 30;
  readonly big = 100n;
  readonly neg = -1;
  private readonly x = 42;
  protected readonly y = "hello";
  readonly [Symbol.iterator] = 0;
}
