# @generated by autocargo from //flow/rust_port/crates/flow_common:flow_common

[package]
name = "flow_common"
version = "0.0.0"
edition = "2024"

[dependencies]
dunce = "1.0.5"
dupe = "0.9.1"
flow_aloc = { path = "../flow_aloc" }
flow_common_leb128 = { path = "../flow_common_leb128" }
flow_data_structure_wrapper = { path = "../flow_data_structure_wrapper" }
flow_lint_settings = { path = "../flow_lint_settings" }
flow_packed_locs = { path = "../flow_packed_locs" }
flow_parser = { path = "../flow_parser" }
jwalk = "0.8.1"
lazy_static = "1.5"
regex = "1.12.4"
serde = { version = "1.0.219", features = ["derive", "rc"] }
serde_json = { version = "1.0.140", features = ["alloc", "float_roundtrip", "raw_value", "unbounded_depth"] }
vec1 = { version = "1.12.1", features = ["serde"] }

[target.'cfg(all(unix, not(target_arch = "wasm32")))'.dependencies]
nix = { version = "0.30.1", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "signal", "term", "time", "user", "zerocopy"] }
