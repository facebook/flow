# @generated by autocargo from //flow/rust_port/crates/flow_edenfs_watcher:flow_edenfs_watcher

[package]
name = "flow_edenfs_watcher"
version = "0.0.0"
edition = "2024"

[lib]
test = false
doctest = false

[dependencies]
flow_common = { path = "../flow_common" }
serde_json = { version = "1.0.140", features = ["alloc", "float_roundtrip", "raw_value", "unbounded_depth"] }

[lints]
rust = { unexpected_cfgs = { check-cfg = ["cfg(fbcode_build)"], level = "warn" } }
