version: 2.1

jobs:
  checkout:
    executor: linux-node
    steps:
      - checkout
      - persist_to_workspace:
          root: .
          paths: .

workflows:
  build_and_test:
    jobs:
      - checkout
