Fix the Flow errors in `main.js`.
Fix the JSX usage so all four Button elements type-check correctly. Do NOT change the Button component definition. Only fix the usage in App.

Do NOT use `$FlowFixMe` or `any`.
- The code must pass `flow check` with zero errors
