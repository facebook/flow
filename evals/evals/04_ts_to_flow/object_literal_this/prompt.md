The file `source.ts` contains a TypeScript program that exposes a small feature
flag registry as an object literal. The registry's methods enable, disable, and
report on flags stored in the same object.

Convert it to idiomatic Flow in `main.js`, preserving the runtime behavior.
