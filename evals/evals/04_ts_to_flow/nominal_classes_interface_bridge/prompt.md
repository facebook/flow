The file `source.ts` contains a TypeScript program that defines two cache
implementations with the same shape and warms them up from a list of key/value
entries, reporting how many entries were newly inserted into each.

Convert it to idiomatic Flow in `main.js`, preserving the runtime behavior.
