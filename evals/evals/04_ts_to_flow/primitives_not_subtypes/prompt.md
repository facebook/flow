The file `source.ts` contains a TypeScript program that walks a sequence of log
lines and reports the length of the longest one, called first on a changelog
array and then on a single commit title.

Convert it to idiomatic Flow in `main.js`, preserving the runtime behavior.
