{
  "metadata": {
    "name": "values_member_unions",
    "category": "ts_to_flow",
    "tags": [
      "flow",
      "ts_to_flow",
      "values",
      "utility_types",
      "keyof",
      "indexed_access",
      "tuples"
    ],
    "difficulty": "medium"
  },
  "grading": {
    "graders": [
      {
        "type": "ast_query",
        "selector": ".type == \"GenericTypeAnnotation\" and .id.name == \"Values\""
      },
      {
        "type": "ast_query",
        "selector": ".type == \"GenericTypeAnnotation\" and (.id.name | type) == \"string\" and (.id.name | startswith(\"$\"))",
        "negate": true
      }
    ]
  }
}
