The file `source.ts` contains a TypeScript program that computes shipping cost
from a quote (weight, distance, and express flag) and totals the cost across
quotes coming from three different sources: an ad hoc literal, an order class,
and a remote provider.

Convert it to idiomatic Flow in `main.js`, preserving the runtime behavior.
