The file `source.ts` contains a TypeScript module exporting three custom React
hooks: a boolean toggle, a generic list container, and a generic debounced
value.

Convert it to idiomatic Flow in `main.js`, preserving the runtime behavior. The
result must pass `flow check` with zero errors.
