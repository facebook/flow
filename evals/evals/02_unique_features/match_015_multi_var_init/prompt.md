Write a Flow function `applyTheme(theme: Theme): string` that uses a single `match` expression to initialize multiple variables (`bg`, `fg`, `accent`) at once, then returns a CSS style string using those variables.

Return a string like `"background: {bg}; color: {fg}; border-color: {accent}"`.
