Write a Flow function `classifyHttpStatus(status: number)` that returns `'info' | 'success' | 'redirect' | 'client_error' | 'server_error' | 'unknown'` using a `match` expression.

Group common status codes together where possible and handle ranges for the rest.
