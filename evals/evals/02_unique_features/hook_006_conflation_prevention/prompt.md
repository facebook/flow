The code in `main.js` has type errors. Fix all the errors while keeping the same overall behavior — the app should still multiply prices, format them as dollar strings, and display them. You may restructure the code, change how the transformations are called, or move logic between hooks and components.

The code must pass `flow check` with zero errors.
