A `DEFAULT_SETTINGS` value is already defined. Write a function `describeSettings(settings): string` that accepts a settings object with the same shape as `DEFAULT_SETTINGS` and returns a short human-readable summary — for example, the theme name and the font size.

Don't write out a separate object type for the parameter. Derive its type from the `DEFAULT_SETTINGS` value so the two can't drift apart.
