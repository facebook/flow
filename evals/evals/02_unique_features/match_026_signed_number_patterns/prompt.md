Write a Flow function `move(position: number, step: Step): number` that uses a `match` expression to move a position along one axis of a grid.

`step` is one of `-1`, `0`, or `1`. Each unit of step moves the position by 8 pixels: a step of `-1` decreases `position` by 8, `0` leaves it unchanged, and `1` increases it by 8.
