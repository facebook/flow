Write a Flow function `tooltipPosition(h: HAlign, v: VAlign): {x: number, y: number}` that returns pixel offsets for positioning a tooltip, matching on both alignment arguments simultaneously.

Use 10 pixels as the base offset (`left` = -10, `center` = 0, `right` = 10; `top` = -10, `bottom` = 10). Cover all combinations exhaustively.
